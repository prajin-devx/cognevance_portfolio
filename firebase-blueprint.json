{
  "entities": {
    "PlannerOrder": {
      "title": "PlannerOrder",
      "description": "Schema representing an academic planner or service order.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "selectedProduct": { "type": "string" },
        "priceInr": { "type": "number" },
        "customNotes": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string" }
      },
      "required": ["name", "email", "selectedProduct", "priceInr"]
    },
    "ContactMessage": {
      "title": "ContactMessage",
      "description": "Schema representing a secure client inquiry transmission.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "subject": { "type": "string" },
        "message": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string" }
      },
      "required": ["name", "email", "subject", "message"]
    }
  },
  "firestore": {
    "planner_orders/{orderId}": {
      "schema": "PlannerOrder",
      "description": "A collection containing academic planner and customized service order details."
    },
    "planner-orders/{orderId}": {
      "schema": "PlannerOrder",
      "description": "An alias collection containing legacy academic planner order details."
    },
    "contact_messages/{messageId}": {
      "schema": "ContactMessage",
      "description": "A collection containing secure incoming client inquiry messages."
    }
  }
}
